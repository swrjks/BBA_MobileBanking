name: phishsafe_sdk
description: "A SDK for banking apps."
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^11.0.0
  shared_preferences: ^2.2.0
  path_provider: ^2.1.2
  device_info_plus: ^9.0.3
  permission_handler: ^11.0.0
  http: ^0.13.6
  tflite_flutter: 0.10.3

dependency_overrides:
  tflite_flutter: 0.10.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/model.tflite

