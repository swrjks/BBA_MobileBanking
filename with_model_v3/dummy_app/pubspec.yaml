name: dummy_bank
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  cupertino_icons: ^1.0.8
  phishsafe_sdk:
    path: /Users/amoteesh/Desktop/Mad2/final_phishsafe_sdk
  tflite_flutter: ^0.10.2  # ✅ no need to upgrade for this workaround

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model_light.tflite  # ✅ moved model from SDK to app
